<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Pge</title>
</head>
<body>
    <h1>Welcome <%= user.name %>  </h1>
    <h3>User : <%=user.user %></h3>
    <h3>Email : <%= user.email %></h3>
    <ul>
        <% user.skills.forEach((item)=>{ %>
            <li><%= item %></li>

        <% }) %>
    </ul>

</body>
</html>